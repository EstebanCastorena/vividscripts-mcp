KAN-98: Hardening sweep + bandit + pip-audit in CI

Closes audit findings #14-#23 from the 2026-05-17 comprehensive repo
audit. Six new failing tests landed first under tests/security/, then
the code fixes, then the durable static-tooling additions.

Findings closed:
- #14 hmac.compare_digest for PKCE verifier (oauth/token.py)
- #15 --seed-session: stderr instead of stdout, host gate, user_id regex
- #16 Defense-in-depth audit-log scrubber (oauth/audit.py)
- #17 Full SHA-256 digest fingerprint, no longer truncated to 64 bits
- #18 Drop user_id (OAuth sub) from MockBackend KeyError messages
- #19 Refresh-token reuse detection with family revocation
- #20 Proper urlparse/parse_qsl/urlunparse merge in mock IdP redirect
- #21 Scope allow-list validator on UserClaims (RFC 6749 known scopes)
- #22 Reject duplicate Authorization headers at the middleware
- #23 Collapse invalid_client to invalid_grant at /oauth/token

Static tooling additions (the lasting win):
- bandit added to .pre-commit-config.yaml and CI (blocking gate)
- pip-audit added as a separate non-blocking CI job
- bandit.yaml configures src/-only scanning with B101 skip
- CLAUDE.md "Quality gates" section documents both tools + nosec policy

5 bandit B105 findings (all OAuth RFC protocol literals - "Bearer",
"access") triaged with # nosec annotations naming the relevant RFC.

Local gates: 565 passed (333 baseline + 232 new), mypy clean, ruff
clean, ruff format clean, pre-commit all-files green, bandit -r src/
0 issues.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
